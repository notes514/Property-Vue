<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.guet.property.mapper.ArticleMapper">

    <insert id="addArticle" parameterType="com.alibaba.fastjson.JSONObject">
        INSERT INTO tb_article
            (content)
        VALUES
            (#{content})
    </insert>

    <select id="countArticle" resultType="Integer">
        SELECT count(0)
        FROM tb_article w
        WHERE w.status = '1'
    </select>

    <select id="listArticle" resultType="com.alibaba.fastjson.JSONObject">
        SELECT
            w.id                                      id,
            w.content                                 content,
            w.version                                  version,
            date_format(w.gmt_create, '%Y-%m-%d %T') gmtCreate,
            date_format(w.gmt_modified, '%Y-%m-%d %T') gmtModified
        FROM tb_article w
        WHERE w.status = '1'
        ORDER BY w.id DESC
        LIMIT #{offSet}, #{pageRow}
    </select>

    <update id="updateArticle" parameterType="com.alibaba.fastjson.JSONObject">
        UPDATE tb_article
        SET
            content = #{content}
        WHERE id = #{id}
    </update>

</mapper>
